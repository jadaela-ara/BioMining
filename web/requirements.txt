# ================================================================
# HYBRID BITCOIN MINING PLATFORM - WEB REQUIREMENTS (FIXED)
# Minimal dependencies for production deployment
# ================================================================

# Core web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# WebSocket support
websockets>=12.0

# HTTP and async support
httpx>=0.25.0
aiofiles>=23.2.0

# Data handling
python-multipart>=0.0.6
pydantic>=2.4.0

# System monitoring
psutil>=5.9.0

# Authentication and security
cryptography>=41.0.0

# Environment and configuration
python-dotenv>=1.0.0

# Additional utilities
requests>=2.31.0

# Scientific computing (minimal for web interface)
numpy>=1.24.0

# Note: hashlib is a Python standard library module, not a pip package
# Note: torch>=2.1.0 removed to avoid build issues (can be added back if needed)